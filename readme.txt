1. in terminal write "cd backend"
2. in terminal write "pip install -r requirements.txt"
if that doesnt work then write "python3 -m pip install -r requirements.txt"
make sure python already install from official python websites